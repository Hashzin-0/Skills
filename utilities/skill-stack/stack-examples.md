# Stack Context Examples

Complete, realistic STACK CONTEXT examples for the most common project types.
Use these as validation that your output matches the expected format.

---

## Example 1: Modern Next.js SaaS (2024-2025)

```
═══════════════════════════════════════
STACK CONTEXT
Generated by: stack-detector
Project: acme-saas
═══════════════════════════════════════

RUNTIME & FRAMEWORK
  Framework:         Next.js 15.1.2
  Router:            App Router
  Language:          TypeScript 5.4
  TypeScript strict: yes
  Package manager:   pnpm

STYLING
  CSS solution:      Tailwind CSS 3.4.1
  Component library: shadcn/ui (components found at src/components/ui/)

STATE MANAGEMENT
  Server state:      TanStack Query 5.28.0
  Client state:      Zustand 4.5.0

DATA & BACKEND
  Database/ORM:      Supabase (direct client + Prisma for migrations)
  API style:         Route Handlers + Server Actions
  Auth solution:     Clerk (clerkMiddleware detected in middleware.ts)

TESTING
  Unit/Integration:  Vitest + React Testing Library
  E2E:               Playwright

CONVENTIONS DETECTED
  Component style:   folder with index.ts (src/components/ComponentName/index.tsx)
  Barrel exports:    yes (index.ts at src/components/, src/lib/)
  Path aliases:      @/* → src/*
  Server components: used extensively (90% of components are RSC)
  Server actions:    present (src/actions/ directory)
  Validation:        Zod (used in all forms and API routes)

IMPORTANT NOTES
  - Project uses Supabase for realtime and storage, Prisma for schema management
  - Clerk is v5 beta — some APIs differ from stable v4 docs
  - No global CSS reset — relies on Tailwind preflight

SKILL ADAPTATIONS REQUIRED
  - design-system: use Tailwind utility classes; import shared UI from src/components/ui/
  - data-access-api: use Supabase client for queries; use Prisma only for migrations
  - isolated-logic: Server Components use async functions — hooks only in Client Components
  - strict-typing: infer types from Zod schemas using z.infer<typeof Schema>
  - security: Clerk handles auth middleware — use auth() from @clerk/nextjs/server in RSC
  - anti-patterns: no direct fetch in components — use Server Actions or TanStack Query mutations

═══════════════════════════════════════
```

---

## Example 2: Next.js Pages Router (Legacy Project)

```
═══════════════════════════════════════
STACK CONTEXT
Generated by: stack-detector
Project: legacy-dashboard
═══════════════════════════════════════

RUNTIME & FRAMEWORK
  Framework:         Next.js 13.5.0
  Router:            Pages Router (no app/ directory detected)
  Language:          TypeScript 5.0
  TypeScript strict: no (only noImplicitAny: true)
  Package manager:   yarn

STYLING
  CSS solution:      styled-components 6.0
  Component library: none detected

STATE MANAGEMENT
  Server state:      SWR 2.2.0
  Client state:      Redux Toolkit 2.0

DATA & BACKEND
  Database/ORM:      Prisma 5.0 + PostgreSQL
  API style:         REST (pages/api/ directory with 23 routes)
  Auth solution:     NextAuth v4.24 (pages/api/auth/[...nextauth].ts)

TESTING
  Unit/Integration:  Jest + React Testing Library
  E2E:               none

CONVENTIONS DETECTED
  Component style:   single file (components/ComponentName.tsx)
  Barrel exports:    no
  Path aliases:      none (uses relative imports)
  Server components: not used (Pages Router)
  Server actions:    not present
  Validation:        Yup (used in forms)

IMPORTANT NOTES
  - Pages Router project — NO Server Components, NO Server Actions
  - getServerSideProps and getStaticProps patterns in use
  - TypeScript strict mode is OFF — types may be looser than ideal
  - No E2E testing configured — code-reviewer should flag this

SKILL ADAPTATIONS REQUIRED
  - isolated-logic: hooks are valid in all components (no RSC restrictions)
  - data-access-api: use Prisma client; API calls through pages/api/ REST endpoints
  - strict-typing: strict mode is off — still enforce types but expect some loose patterns already
  - design-system: use styled-components pattern; no Tailwind
  - security: NextAuth v4 patterns — use getServerSession() not auth()

═══════════════════════════════════════
```

---

## Example 3: React SPA with Vite

```
═══════════════════════════════════════
STACK CONTEXT
Generated by: stack-detector
Project: react-dashboard
═══════════════════════════════════════

RUNTIME & FRAMEWORK
  Framework:         React 18.3 + Vite 5.2
  Router:            N/A (client-side only — React Router v6)
  Language:          TypeScript 5.4
  TypeScript strict: yes
  Package manager:   npm

STYLING
  CSS solution:      Tailwind CSS 3.4
  Component library: Radix UI (primitives, no shadcn wrapper)

STATE MANAGEMENT
  Server state:      TanStack Query 5
  Client state:      Zustand 4

DATA & BACKEND
  Database/ORM:      none (consumes external REST API)
  API style:         REST (external API at VITE_API_URL)
  Auth solution:     custom (JWT stored in localStorage, src/lib/auth.ts)

TESTING
  Unit/Integration:  Vitest + React Testing Library
  E2E:               Playwright

CONVENTIONS DETECTED
  Component style:   folder with index.tsx
  Barrel exports:    yes
  Path aliases:      @/* → src/*
  Server components: not used (client-side SPA)
  Server actions:    not present
  Validation:        Zod

IMPORTANT NOTES
  - Pure client-side SPA — no SSR, no server components, no API routes
  - Auth is custom JWT implementation — security skill should review token handling
  - External API base URL from VITE_API_URL env variable

SKILL ADAPTATIONS REQUIRED
  - isolated-logic: all components can use hooks freely (no RSC)
  - data-access-api: all API calls through TanStack Query + custom fetch wrapper at src/lib/api.ts
  - security: JWT in localStorage is a security concern — flag for review but don't break existing pattern

═══════════════════════════════════════
```

---

## Example 4: Monorepo (Turborepo + Next.js)

```
═══════════════════════════════════════
STACK CONTEXT
Generated by: stack-detector
Project: acme-monorepo → target: apps/web
═══════════════════════════════════════

MONOREPO STRUCTURE
  Tool:              Turborepo
  Packages:          apps/web (Next.js), apps/api (Express), packages/ui, packages/types, packages/config
  Target package:    apps/web
  Shared UI:         packages/ui (import from @acme/ui)
  Shared Types:      packages/types (import from @acme/types)

RUNTIME & FRAMEWORK
  Framework:         Next.js 15.0 (in apps/web)
  Router:            App Router
  Language:          TypeScript 5.4
  TypeScript strict: yes (extends @acme/config/tsconfig)
  Package manager:   pnpm workspaces

STYLING
  CSS solution:      Tailwind CSS 3.4
  Component library: Custom (packages/ui — import as @acme/ui)

STATE MANAGEMENT
  Server state:      TanStack Query 5
  Client state:      Zustand 4

DATA & BACKEND
  Database/ORM:      Drizzle ORM + PostgreSQL (in apps/api)
  API style:         tRPC (type-safe, from apps/api)
  Auth solution:     Lucia (shared between apps)

TESTING
  Unit/Integration:  Vitest (per package)
  E2E:               Playwright (root level)

CONVENTIONS DETECTED
  Component style:   folder with index.tsx
  Barrel exports:    yes (per package)
  Path aliases:      @acme/* → packages/*/src; @/* → apps/web/src/*
  Server components: used extensively
  Server actions:    present
  Validation:        Zod

IMPORTANT NOTES
  - Do not add dependencies directly to apps/web without checking packages/ui first
  - Shared types in @acme/types must be updated for any new data models
  - tRPC client already configured at apps/web/src/lib/trpc.ts

SKILL ADAPTATIONS REQUIRED
  - data-access-api: use tRPC mutations/queries, not raw fetch; client at @/lib/trpc
  - design-system: UI components come from @acme/ui — extend there, not locally
  - modularization: shared logic goes in packages/, not apps/web/src/

═══════════════════════════════════════
```
